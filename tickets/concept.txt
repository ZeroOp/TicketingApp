We Now neede the nats client that to setup the publishers and consumers. We might think to connect it in the index.ts that makes more sense. 

There is a distinction between NATs client and the Mongoose. 

Mongoose internally keeps the record of all the internal connections that are in use. 

We can open any other file in our project and can import the mongoose. 

Natstreaming directory works very differntly. 

when we use nats.connect it will give us instance. We might need to share that instance across all files in our repository. 

mongoose internally make the connection. 

But that is an issue because our TicketCreatedRouteHandler is imported in app.ts which is again imported into index.ts. 

We need create the nats client and we need to export that and import into ticketCreatedRoute Hanlder. 

Now we are creating a loop here. 

index requires TicketCreatedRouteHandler. And TicketCreatedRouteHandler Needs Nats client from index.ts which will create a circtular dependency. 

So we need create a nats-client in index.ts but that should be imported in TicketCreatedRouteHandler but not from the index.ts .

How to solve this? 

Create a new file called nats-client.ts 

It will be single source something like mongoose. 

Index.ts file will import that and connect to the nats-client.ts. 

We will initialize the natsclient in index.ts but we might need to import that initialized file into TicketCreatedRouteHandler. 


Se cann't wait for the vent bus to send event to event bus because that can be failed at some or other point of time. 

what we can do is we can store that event into some kind of database . Transactions database which having events collection. 

We have Sent flag and Events in the database. 

And we can have a separate code/process watching events .that code will take the events from the events collection and publish that event to NATS.

If there is any issue with connecting to our database , then we fail to make the transaction to the transaction database as well. 

If connection to the nats is down for some point of time , when the nats connection comes back , hopefully our events will be sent to the nats server eventually. 

We need to make sure if we make the changes transactions collection will be updated only if the events collection is updated. And vice versa. We need to make sure that if any one of this query fails. 

We need to revert whole transaction. 

We need to roll back. 